{
  "name": "promptlingo",
  "version": "1.0.0",
  "description": "A web-based speech-to-speech translation application for English ↔ Haitian Creole",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "build": "npm run build:wasm && cd client && npm run build",
    "build:netlify": "npm run build:wasm && cd client && npm install && npm run build",
    "build:render": "npm run build:wasm && cd client && npm install && npm run build",
    "start": "cd server && npm start",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install",
    "migrate:render": "./migrate-to-render.sh",
    "build:wasm": "cd wasm-text-processor && ./build-all.sh",
    "build:wasm:web": "cd wasm-text-processor && ./build-web.sh",
    "build:wasm:nodejs": "cd wasm-text-processor && ./build-nodejs.sh",
    "wasm:check": "cd wasm-text-processor && cargo check"
  },
  "keywords": [
    "translation",
    "speech",
    "haitian-creole",
    "ai",
    "openai",
    "elevenlabs"
  ],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2",
    "netlify-cli": "^23.9.2"
  },
  "dependencies": {
    "pg": "^8.16.3"
  }
}
